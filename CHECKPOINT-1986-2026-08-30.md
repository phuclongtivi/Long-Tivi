# Project 1986 Checkpoint

Date: 2026-08-30

Project name: 1986

## Continuity Note

When the user says "1986", treat it as this Long app project and surface the latest exchange plus the relevant prior history:

- The app source came from `Long-optimized-source-2026-08-30.zip`.
- Stage 2 AI vision/live object interaction was added conceptually and in source before this checkpoint.
- Home and Chat were merged so chat rooms can appear as water-drop subrooms attached to Home event notices.
- `superBUY` was moved to the old Chat tab position and its square frame was removed.
- LIVE was simplified to one create-live flow through the Mixer.
- The app now saves the latest Mixer preset for the user and shows it in Menu.
- The "Vao Rap" video browsing experience was optimized for smoother, lighter swipe behavior.
- Graphic Optimization v2 was applied: youthful, clean, refined, soft sci-fi design language with glass surfaces, cyan/blue/violet glow, stronger video stage, upgraded Home feed, LIVE/Mixer, and chat drops.

Latest generated source package before this checkpoint:

- `Long-graphic-optimization-v2-2026-08-30.zip`

Latest generated source packages after follow-up updates:

- `Long-menu-superbuy-locker-2026-08-30.zip`
- `Long-1986-full-checkpoint-updates-2026-08-30.zip`

## User Request To Remember

The user approved the next work package and first requested no code changes, then later asked to include those saved checkpoint items in the next ZIP:

1. Implement full i18n for 3 languages.
2. Improve App Store readiness.
3. Add Mascot v2 redesign and apply it across the app.
4. Save this checkpoint for the next update cycle.

## i18n Scope For Next Code Pass

The app already has a basic i18n foundation:

- `lib/i18n/dictionaries.ts`
- `lib/i18n/detectLocale.ts`
- `components/LanguageProvider.tsx`
- `components/LanguageSwitcher.tsx`

Supported languages should remain:

- Vietnamese: `vi`
- English: `en`
- Chinese: `zh`

Next pass should expand from partial coverage to full product coverage:

- Bottom navigation
- Home feed
- Home event notices
- Global chat and event chat drops
- LIVE action bar
- LIVE Mixer
- "Vao Rap" / cinema mode
- Store / superBUY
- Menu / Dashboard
- Auth prompts
- Payment, ticket, order, gift, notification screens
- AI assistant messages where text is UI-facing
- Error/loading/empty states

Implementation preference:

- Keep Vietnamese as default.
- Continue detecting browser language.
- Keep the manual language override in local storage.
- Move hard-coded UI strings into dictionary keys.
- Avoid changing business logic while adding translations.
- Use concise translations to prevent layout overflow on mobile.

## App Store Readiness Scope For Next Code Pass

Current state:

- The project has `capacitor.config.ts`.
- Native `ios/` project was not present in the current source at checkpoint time.
- `capacitor.config.ts` uses `webDir: 'out'`, while current Next config does not yet show a completed static export setup.
- `next.config.js` currently skips TypeScript and lint validation during build.
- The app uses features that need careful Apple review preparation: camera, microphone, location, push notifications, livestream, chat, AI moderation, gifts, store, ticket/payment flows.

Next pass should prepare an App Store readiness layer:

- Add an `APP-STORE-READINESS.md` checklist.
- Decide packaging path: Capacitor live server URL vs static export.
- Prepare required iOS permission strings for camera, microphone, photo library, location, notifications, and tracking if used.
- Document privacy policy requirements.
- Document Terms of Use requirements.
- Add or verify Report/Block user flows for livestream/chat.
- Review payment flows for Apple In-App Purchase risk, especially digital tickets, gifts, stickers, points, and AI quota.
- Review user-generated content requirements: moderation, reporting, blocking, appeal/escalation.
- Remove or reduce `ignoreBuildErrors` and `ignoreDuringBuilds` when preparing final App Store submission.
- Verify on real iPhone before submission.

## Mascot v2 Scope For Next Code Pass

Current state:

- Mascot is already applied in the app.
- Main mascot files:
  - `public/ai-mascot-full.jpg`
  - `public/ai-mascot-round.jpg`
  - `public/stickers/phuc-chatbot-avatar.png`
  - `public/stickers/phuc-chatbot-avatar.webp`
  - `public/stickers/phuc-chatbot-logo.webp`
- Mascot render points include:
  - `components/AdminAIChatbot.tsx`
  - `components/event/AiMascot.tsx`
  - `components/event/AiIdlePresence.tsx`
  - `components/event/ChatRoomWindow.tsx`
  - `components/event/NotifyInbox.tsx`
  - `components/event/LiveTapChatOverlay.tsx`
  - `components/event/PhucChatbotAvatar.tsx`

Design issue:

- The current mascot visual language is still the old red/white/simple placeholder style.
- It does not fully match Graphic Optimization v2: glass UI, cyan/blue/violet glow, youthful refined sci-fi mood.

Next pass should create and apply a Mascot v2 system:

- Redesign AI Phuc as a friendly soft sci-fi assistant mascot.
- Keep it approachable, youthful, clean, and premium.
- Create at least:
  - full body mascot
  - round avatar
  - small icon/avatar for chat bubbles
  - transparent sticker/reaction assets
- Export optimized WebP/PNG files.
- Keep old file paths as fallback or update references safely.
- Apply the new mascot consistently to chatbot, LIVE idle state, notifications, chat rooms, and object/tap overlay.
- Make sure the mascot works in both light and dark themes.
- Keep file sizes light enough for mobile.

## Suggested Order For Next Update

1. Finish full UI dictionary coverage for `vi/en/zh`.
2. Add language QA pass for common flows.
3. Add Mascot v2 assets and apply them across the app.
4. Add App Store readiness docs and native configuration notes.
5. Fix the highest-risk TypeScript/lint blockers that affect App Store packaging.
6. Build and package a new ZIP for GitHub upload.

## Implemented In Current Code Pass

- Expanded 3-language i18n (`vi`, `en`, `zh`) for the highest-traffic app surfaces.
- Added translated variable strings through `formatT()` / `tf()`.
- Connected bottom navigation, Home, LIVE action bar, quick live create flow, Mixer labels, superBUY locker header, and Menu sections to i18n.
- Added Mascot v2 PNG/WebP assets and changed shared mascot entry points to use the new assets.
- Kept old mascot files as fallback.
- Added `APP-STORE-READINESS.md`, `I18N-3-LANGUAGES.md`, and `MASCOT-V2.md`.
- Updated PWA manifests to Graphic Optimization v2 colors.
- Updated in-app legal guide copy from the old 6-tab structure to the current 5-tab structure.

## Remaining Before App Store Submission

- Continue moving lower-frequency hard-coded copy into i18n keys.
- Create native `ios/` project and add Info.plist permission strings.
- Remove or resolve `ignoreBuildErrors` and `ignoreDuringBuilds` before final Apple submission.
- Verify report/block/moderation/payment flows on a real iPhone.

## Visual Audit And Migration Pass

User request: check whether old writing, icons, colors, frames, images, animation, background, or graphics from the earlier design remain, and redesign them to match the new design language.

Findings:

- Old beige/brown/red palette still appeared in secondary screens and components:
  - `#8B4513`
  - `#D4C9B5`
  - `#F5F0E6`
  - `#FAF7F0`
  - `#EDE6D9`
  - `#FFF7ED`
  - `#C41E3A`
  - `#DC2626`
- Some fallback theme tokens still used the old navy-first skin.
- The floating AI chatbot still used the old beige panel and red/brown buttons.
- `LanguageSwitcher` still used white/red styling.
- `LiveObjectStudio` still used beige panel, red product tags, white text tags, and old form controls.
- The livestream gradient text still used a multi-color rainbow animation instead of the new cyan/blue/violet sci-fi range.
- Some old mascot/logo assets remain in `public/` intentionally as fallback compatibility files.

Implemented:

- Updated `app/globals.css`:
  - Removed negative letter spacing.
  - Replaced old rainbow livestream text animation with cyan/blue/violet sci-fi gradient.
- Updated `components/event/theme.css`:
  - Migrated core light tokens to `#f8fbff`, `#eef4ff`, `#10172a`, `#2563eb`, `#22d3ee`, `#8b5cf6`.
  - Migrated dark tokens to a deeper sci-fi blue/black base.
  - Replaced old navy fallback values where they affected UI.
  - Added a visual migration layer that maps old beige/brown/red backgrounds, borders, and Tailwind red classes into the new glass/cyan/blue/violet language.
  - Added sci-fi glass styles for chatbot, language switcher, AI Vision panel, hologram buttons, and object tags.
  - Kept reduced-motion support.
- Updated `components/AdminAIChatbot.tsx`:
  - Replaced old beige chat shell with `pl-ai-chat-panel`.
  - Replaced old black/red/brown UI pieces with shared sci-fi chat classes.
  - Uses Mascot v2 avatar component.
- Updated `components/LanguageSwitcher.tsx`:
  - Replaced red/white pills with `pl-language-switcher`, `pl-lang-on`, and `pl-lang-off`.
- Updated `components/LiveObjectStudio.tsx`:
  - Replaced beige control panel with `pl-vision-panel`.
  - Replaced old red/white draggable item tags with hologram-style `pl-vision-tag`.
  - Replaced old buttons/form fields with `pl-holo-button`, `pl-mini-button`, and `pl-field`.
- Updated `components/MixerPresetMenuCard.tsx`:
  - Replaced old red button with `pl-holo-button`.
  - Uses `pl-menu-module` glass panel.
- Updated `components/LanguageProvider.tsx`:
  - Replaced the old beige loading fallback with `pl-future-shell`.

Verification:

- Ran `npm run build` successfully after the visual migration pass.
- Next build still skips TypeScript and lint validation because `next.config.js` has `ignoreBuildErrors` and `ignoreDuringBuilds`. This is unchanged and still listed as an App Store readiness item.

Notes:

- Old visual asset files such as the original mascot/avatar/logo remain in `public/` only as fallbacks. Shared mascot entry points already point to Mascot v2.
- Some Vietnamese product/legal/content strings still mention app concepts like `superBUY`; those are content text, not old visual skin. They should be handled in the continuing i18n/legal copy pass, not by the graphic migration layer.
- A source grep still finds old palette literals in lower-frequency screens such as login, cart, orders, seller, map, admin panels, and gift/payment panels. The current update covers them at render time through the migration CSS layer. A later cleanup pass can remove those old inline literals from the source one file at a time, but the user-facing skin is now pulled into the new design language.

## Button / CTA / Route Refactor Pass 1

User request: audit all functional buttons, their logic and continuity across the app, align positions/copy/icons, prioritize quick login, add a light diamond sci-fi polish, and use the red Phúc Long dragon as the device app logo.

Implemented:

- Unified the LIVE creation entry point:
  - `/live` redirects to `/?pane=create`.
  - Dashboard “Bắt đầu Live” opens the same Mixer-first flow.
- Kept Chat merged into Home:
  - Favorites' old Chat redirect now opens `/home?inbox=open`.
  - Chat-room back navigation returns to Home Chat with the correct label.
- Updated live detail/map/vote and auth callback shells to the shared future/glass design system.
- Normalized key action, back-navigation, authentication and chat-composer styles.
- Moved quick biometric login to the first position above every social/email method and added a visible “Ưu tiên” status.
- Added keyboard focus-visible, touch feedback, hover and reduced-motion rules.
- Added shared diamond-metal tokens and a light sci-fi glint layer to titles, tabs, buttons, Mixer, store cards, locker/inventory, gift/menu modules and common cards.
- Regenerated device icon assets from the existing red Phúc Long dragon logo:
  - `app/icon.png`
  - `app/apple-icon.png`
  - `app/favicon.ico`
  - `public/icon-192.png`
  - `public/icon-512.png`
  - `public/apple-touch-icon.png`
- Added explicit Next.js icon metadata in `app/layout.tsx`.
- Added full audit notes in `BUTTON-AUDIT-REFACTOR-PASS-1.md`.

Verification:

- `npm run build` passed.
- Next.js production compilation passed.
- Static generation passed for 30/30 pages.

Pass 2 backlog:

- Add explicit `type="button"` to remaining legacy buttons that still rely on browser defaults.
- Complete low-frequency `vi/en/zh` copy extraction.
- Remove old inline palette literals directly from source after UI migration coverage is no longer required.
- Run authenticated end-to-end browser tests for LIVE creation, Home Chat room creation, checkout, gifting, moderation and Boss controls.
- Re-enable TypeScript and ESLint build gates after resolving the existing legacy issues.
